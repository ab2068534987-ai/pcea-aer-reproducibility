# Alibaba raw data placeholder

Raw Alibaba CSV files are not included in the cleaned repository.

If you need to rebuild the processed dataset, place the raw files here locally:

- `batch_task.csv`
- `batch_instance.csv`

The final checked-in experiment uses `datasets/alibaba_pcea/processed_benchmark/`.
